#include<stdio.h>
int main(){
    int num,tem,rem,sum=0;
printf("Enter a Number : ");
scanf("%d",&num);
tem=num;
do{
  rem=num%10;
  sum=sum+rem;
  num=num/10;
}while(num!=0);
if(tem%sum==0)
printf("\n%d is harshad number",tem);
else
    printf("not harshad number");
}
